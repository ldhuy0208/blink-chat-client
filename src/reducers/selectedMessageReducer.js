const initState = {
  current: {
    name: "le duc huy",
    avatar:
      "https://img.freepik.com/free-vector/businessman-character-avatar-isolated_24877-60111.jpg?size=338&ext=jpg",
  },
  isGroup: false,
};

const selectedMessageReducer = (state = initState, action) => {
  switch (true) {
    default:
      return state;
  }
};

export default selectedMessageReducer;
